defmodule GodwokenExplorer.Graphql.Resolvers.Statistic do
  # TODO: add home_page_statistic
  def home_page_statistic(_parent, _args, _resolution) do
    {:ok, nil}
  end

  def erc20_statistic(_parent, _args, _resolution) do
    {:ok, nil}
  end
end
